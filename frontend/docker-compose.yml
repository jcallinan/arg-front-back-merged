version: "3.8"

services:
   arg-web-react:
      image: argcicd.azurecr.io/dev/arg-web-frontendacr:latest
      container_name: arg-web-frontend
      restart: always
      ports:
         - "5173:5173"
      environment:
         - VITE_PORT=5173
         - VITE_API_BASE_URL=http://qa-damco.amref.com:5001/v1
         - VITE_SOCKET_HOST=http://qa-damco.amref.com:5001
         - VITE_ENV=uat
