export enum ProcessType {
  FLEXI = "flexi",
  SOGAS = "sogas",
  PAPER = "paper",
  LMS = "lms",
  CLEAR_CHECKS = "clear-checks",
}

export enum InvoiceType {
  PAPER = "paper",
}

export enum SogasSubType {
  REGULAR = "regular",
  TAX = "tax",
}
